<%= form_with(model: weapon_type) do |form| %>
  <% if weapon_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(weapon_type.errors.count, "error") %> prohibited this weapon_type from being saved:</h2>

      <ul>
        <% weapon_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
